export function EventsPage() {
  return (
    <section className="page">
      <h1>Events</h1>
      <div className="card">
        <p>
          Events list will be wired after backend adds <code>GET /events</code> and{' '}
          <code>GET /events/:id/runs</code>.
        </p>
      </div>
    </section>
  )
}

